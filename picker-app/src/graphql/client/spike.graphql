query Spikes($input: SpikesInput!) {
    spikes(input: $input){
        ...SpikeBase
    }
}

query Spike($input: SpikeInput!) {
    spike(input: $input){
        ...Spike
    }
}

fragment SpikeBase on SpikeBase {
    id,
    name,
    nameEn,
    allWeatherOnly,
    angleScore,
    brand,
    events {
        ...ITAFShoeEvents
    },
    gripScore,
    hardnessScore,
    colorImages {
        ...ITAFShoeImages
    }
    lightnessScore,
    price,
    score,
    weight,
    widthScore,
}

fragment Spike on Spike {
    id,
    name,
    nameEn,
    allWeatherOnly,
    angleScore,
    brand,
    events {
        ...ITAFShoeEvents
    },
    gripScore,
    hardnessScore,
    colorImages {
        ...ITAFShoeImages
    }
    lightnessScore,
    price,
    score,
    weight,
    widthScore,
}

fragment ITAFShoeImages on ITAFShoeColorImages {
    colorId,
    colorCode,
    imageUrls
}

fragment ITAFShoeEvents on ITAFShoeEvents {
    id,
    label
}
